{
  "name": "swordmaster",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "소드마스터의 길 — 제스처 검술 액션 로그라이크 (GDD 기반 신규 개발)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "phaser": "^3.85.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
