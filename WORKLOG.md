# WORKLOG — 소드마스터의 길

에이전트 간 인수인계 장부. 작업 시작 전 최근 10줄 + `git log --oneline -5`를 읽고,
작업 종료 시 한 줄 추가: `- YYYY-MM-DD [에이전트/도구] 무엇을 · 왜 · 미완성/주의사항`.

---

- 2026-07-10 [Claude/Opus] **T1-05 검술 6종 + 전투 수치**(설계 확정분대로).
  · techniques.json에 6종 조합·window·cost·**base 데미지**(일자18/십자22+경직/연풍28광역/낙뢰45/회천40광역/섬광60관통) + balance.json 기본획 데미지 10.
  · 다중 검술 트래커(`combo.ts` 재작성): 접두어 공유 처리(긴 조합 우선 — 회천무>일자베기), 기본획 노이즈 prune, 진행 중 미스=발동 실패.
  · 데미지 = base × (평균≥95?1.2:1.0) × 검결(all-or-nothing, 전 획 검결+보너스 시 ×1.1). balance `commandBonusMode` 플래그로 proportional 전환 가능(로직 분리).
    미스 페널티 = 마나 30%(=cost×0.3) 소실 + 경직 800ms. 마나 = cost×(2−평균/100).
  · 데미지 표(퍼펙트): 일자22/24·십자26/29·연풍34/37·낙뢰54/59·회천48/53·섬광72/79(검로/검결). 데미지÷코스트 1.9~2.4(아츠 효율 우위 의도 충족).
  · 게임 HUD: 진행 중 검술 후보 표시("일자베기 1/2 · 십자베기 1/2 · 연풍참 1/2"), 성립 시 데미지·마나·플래그(위력+20%/검결+10%/경직/광역/관통), 실패 시 페널티.
  · 검증: vitest 87종 통과(+검술 성립6·데미지공식·우선순위·미스페널티·데미지표), 빌드 OK, 브라우저 자가진단 28/28 + 연풍참(검로 34) + 낙뢰참(검결 59) + 미스 페널티(−3·800ms) 실기. 라이브.
  · 주의: 미스 페널티의 대상 검술은 진행 중 접두어 후보 중 최장(공유 접두어라 모호할 수 있음)로 선정 — 문서화. 실데미지의 적 HP 적용은 T1-07 전투.

- 2026-07-10 [Claude/Opus] **T1-04 유파 정식화**. `src/engine/style.ts` StyleManager 신설.
  · **아키텍처 판단(게임플레이 불변, 명시)**: 미러는 좌표 기반(입력을 canonical=우수로 접기) 유지. v2가 제안한
    "획ID 매핑 테이블로 단순화"는 원(wonmu) 손방향(chirality)에서 어긋나므로 채택 안 함(좌표 미러가 더 정확).
    → 판정 strokeId는 항상 canonical이라 **검술 조합을 canonical로 정의하면 두 유파에서 자동 동작**(연풍참 좌수판 별도 오버라이드 불필요).
  · StyleManager: 미러 획ID맵(h_lr↔h_rl·diag_dr↔diag_dl, 나머지 자기자신), techniqueCombo(오버라이드 지점—우수/좌수는 비어 있음, 미러 아닌 미래 유파용), updownCluster, otherStyle. 게임 유파 토글을 otherStyle로 연결.
  · 검증: 8획 전부 좌수(미러 입력)에서 canonical 판정, 원무 좌수 chirality 정상. vitest 72종 통과, 빌드 OK.
  · 주의: 완료기준의 "검술 6종 좌수 동작"은 검술이 T1-05에서 생기므로 그때 실증(메커니즘=fold-to-canonical이 보장).

- 2026-07-10 [Claude/Opus] **T1-03 검결 8종 + 리듬 + OI-12**. (커맨드는 strokes.json에 이미 정의 — →←/↓+←/↓↑/→→/원무 →↓←↑.)
  · **버그 수정(중요)**: judgeRhythm이 동시입력(chord ↓+→)의 순서를 엄격히 봐서, →가 ↓보다 먼저 들어오면 order-miss.
    → 동일 이상오프셋(동시) 연속 토큰을 "그룹"으로 묶어 그룹 내 순서 무관 멀티셋 매칭. 실측 사선 성공률 62%→~100%.
  · **OI-12 결론(반전)**: 원무(4입력)는 어렵지 않음 — 4개 순차 비트(400ms 간격)라 여유 큼(성공 굿+ σ30/50/70=100/100/99.9%).
    오히려 **동시입력(chord 사선)이 취약** — 두 키가 simulMs(70) 밖이면 대각 결합 실패. 성공 σ30/50/70 = 100/99.1/94.3%.
    즉 v2가 우려한 원무 난이도는 문제 아님. 튜닝 여지는 simulMs(chord 관용도)이며, 넓히면 빠른 순차 오결합 위험 — 운영자 판단 대기.
  · 검술별 rhythm의 techniques.json 이동: 단일획 command 리듬은 획 고유이므로 strokes.json 유지가 자연스러움. 검술(combo) 레벨 리듬은 T1-05 검술 구현 시 다룸(구조 판단, 게임플레이 불변).
  · 검증: vitest 59종 통과(+8커맨드/8리듬/OI-12 측정), 빌드 OK, 브라우저 자가진단 28/28 + 원무 키보드 퍼펙트(오차1ms·위력+10%) + 순서뒤바뀐 chord 퍼펙트. 라이브.

- 2026-07-10 [Claude/Opus] **T1-02 획 8종 완성(검로) + 형태/lenBand 판정**. 설계 담당 확정분대로 구현.
  · shapeType(line/circle) 분기: line=직선도 (현/경로)² 유지. circle=원형도[반지름CV 50%+각도스윕(330°만점) 30%+폐합 20%],
    방향 40%는 회전방향 일치(우수=시계, 원무 템플릿 부호 자동)+단조성 — 반시계=0. 원 게이트=스윕≥200°+CV≤0.35+폐합≤0.6(호/스파이럴 배제).
  · lenBand(캔버스대각 대비 길이비) 동률 판별: 찌르기[0.035,0.16] vs 횡베기[0.16,1.0]. 찌르기 speedMs 60~300·minLenRatio 0.05 오버라이드.
    오버라이드 일반화(speedMs/minLenRatio/lenBand, 미지정 시 balance 전역) — 전부 strokes.json, 코드 하드코딩 없음. 사전거부 바닥=템플릿 최소 minLenRatio.
  · 추가 5획: 횡베기(우→좌) h_rl / 사선↙ diag_dl / 올려베기 v_up / 찌르기 thrust / 원무 wonmu(24점 시계 원). 원무 command 초안 →↓←↑(T1-03 확정).
  · **오분류율(합성 N=400/획, node vitest)**: 8획 자기분류 100%(찌르기 99.5%). **찌르기→횡베기 0.5% / 횡베기→찌르기 0.0% / 원무↔사선(↘↙) 양방향 0.0%.**
  · 발견: 8방향+원 커버리지에서 "획 불명"은 드문 폴백(45°만 벗어난 ↖/↗는 인접 획 스냅, 곡선은 원/최근접 흡수). 트레이드오프: 경계 근처 짧은 횡베기가 찌르기로 갈 수 있으나 실측 0%.
  · 검증: vitest 42종 통과(회귀 29 갱신+T1-02 신규+혼동행렬), 빌드/타입 OK, 브라우저 자가진단 28/28, 실기 원무 그레이트 93. 라이브 배포.
  · **T1-03 자동진행 안 함** — 경계값(lenBand/circleMinSweep 등) 조정 필요 시 운영자 판단 대기.

- 2026-07-10 [Claude/Opus] **T1-01 프로젝트 전환 + 엔진 분리**. Vite + TS + (Phaser dep) 셋업.
  · 순수부를 `src/engine/*.ts`로 이식(types/geometry/judge/command/rhythm/combo/data + index 배럴), DOM 계층과 분리.
  · 데이터 JSON 외부화: `src/data/{balance,strokes,techniques,styles}.json` → `engine/data.ts`에서 로드.
  · **M0 회귀 29종을 vitest로 이관**(`src/engine/engine.test.ts`) → `npm test` 통과. tsc 타입체크 통과, `npm run build` OK(JS 18KB).
  · 게임 DOM 계층을 `src/main.ts`(TS, 엔진 임포트) + `src/style.css` + Vite `index.html`로 포팅. 빌드 번들에서 자가진단 24/24, 혼합입력 연풍참(평균100·위력+20%) 실증.
  · **Pages 파이프라인 전환**: `.github/workflows/deploy.yml`(npm ci→build→dist upload→deploy-pages), Pages 소스 branch/root → **GitHub Actions(build_type=workflow)**. Phaser는 의존성만 추가(씬은 T1-02+).
  · 완료 기준 충족: 29 회귀 새 구조 통과 + Pages가 Vite 빌드로 동작. 미완/주의: Phaser 씬 미착수(다음), CI 첫 배포 확인 필요.

- 2026-07-10 [Claude/Opus] **T1-00 문서 갱신(M1 첫 작업)**. repo `docs/` 신설·정착.
  · 발견: 이전 세션 언급 원본(설계서 v3.0/SRS v1.1/작업지시서 v1)이 이 PC·repo에 부재 → M0 검증 통과분을 단일 진실로 정식 문서 재정착.
  · 신설: `docs/설계서_v3.1.md`(방향=국소 일치도, 직선=(현/경로)², 커맨드 확정 640/즉시확정, occupancy 중재, 수치표 §2.4, 변경이력),
    `docs/SRS_v1.2.md`(FR-INP-004 정정, FR-INP-014 +10% 조건, OI-12=M1검증/OI-16=메커니즘 검증완료), `docs/작업지시서_v2.md`(정착), `docs/README.md`(색인).
  · repo 루트 `CLAUDE.md` 신설: 현재 상태 M0완료/M1착수 + 작업 규칙. 루트 README 상태 갱신.
  · 설계 변경 6건(승인) 전부 문서 반영. 미완/주의: 원본 v1 계열 파일은 부재 → 신설판이 canonical(문서에 명시).

## M0 검증 리포트 (T0-11) — 2026-07-10

### (a) M0 DoD(완료 정의) 체크리스트
| 항목 | 상태 | 근거 |
|---|---|---|
| 획 3종 검로(제스처) 판정 | ✅ | h_lr/diag_dr/v_down, node 판별 + 라이브 실기(퍼펙트/불명 구분) |
| 획 3종 검결(커맨드) 판정 | ✅ | ←→ / ↓+→ / ↑↓, 키보드+가상패드 |
| 5단계 등급(미스~퍼펙트) | ✅ | 검로=궤적 정확도, 검결=타이밍 정밀도 |
| 등급별 사운드(검명 5종)+햅틱 | ✅ | T0-07, 음색 구분 |
| 메트로놈(목검 비트) | ✅ | T0-05, 토글+비트펄스 |
| 이상 vs 실제 비교 오버레이 | ✅ | T0-08, "납득 가능한 실패" |
| 연풍참 성립 + 마나 공식 | ✅ | 검로/검결/혼합 모두, 마나=15×(2−평균/100) |
| 좌수검류 미러(품질 동등) | ✅ | T0-10, 우수=좌수 accuracy 동일 |
| 입력 채널 중재(동시 활성) | ✅ | T0-06, 먼저 든 채널 점유 |
| 세로 방향 안내(가로 고정) | ✅ | orientation 오버레이 |
| **테스터 3인 "다시 긋고 싶다"** | ⬜ **미실시** | 실사용자 플레이테스트 필요(자체검증 불가) |
| 실기 검증(폰/PC 실제 손) | △ 부분 | 개발자 합성·수동 확인 완료, 3인 실기 미실시 |

검증 자산: node 회귀 **29/29**, 브라우저 자가진단 **24/24**, 라이브 https://hermit7999.github.io/swordmaster-road/ .
OI-12(원무 커맨드 난이도)는 원무가 M1 범위라 M0 DoD 아님(보류).

### (b) 설계·수치 변경 전체 목록 (문서 반영용)
**산식/로직 변경 (SRS·설계서 반영 필요):**
| # | 항목 | 변경 전(문서/초안) | 변경 후 | 사유 |
|---|---|---|---|---|
| 1 | 방향 점수(40%) | 순(net) 시작→끝 벡터 | 구간별 국소 방향 일치도 평균 | 순 벡터는 좌→우 흔들림에 방향 만점→배드 도달 불가(dir40+속15+완15=70 바닥). **승인됨** |
| 2 | 직선도(30%) | (문서 미명시) | (현/경로)² (정규화 공간) | 흔들림 감점 강화, 등급 구배 확보 |
| 3 | 커맨드 확정 | 260ms 초안 대기 | **완전일치 즉시확정** + 대기 640ms | 정박(400ms)>대기(260)라 2번째 비트 전 버퍼 쪼개짐 → 정박 플레이 불가였음 |
| 4 | 입력 중재 | arbiterLockMs 110(시간 blanket) | **occupancy 모델** + staleMs 2000 | 빠른 연속 검결의 2번째가 HUD 삼킴 → 채널 점유로 대체 |
| 5 | 제스처 좌표 | offsetX | clientX−rect | 변환/DPR 안전 + 합성이벤트 테스트 가능 |
| 6 | 검결 정확도 | T0-04 임시(tightness) | RhythmJudge 실판정 | T0-05 완성으로 교체 |

**신규 수치 (문서에 없던 M0 설계값 — 문서에 신규 등재 필요):**
- `rhythm.beatMs = 400` (검결 리듬 템포, ms) · `rhythm.windows` = 퍼펙트50/그레이트100/굿180/배드280(문서 §2와 일치)
- `simulMs = 70` (동시키 인식 창) · `commandWindow = 1500` (순차 입력 창) · `commandResolveMs = 640` · `arbiterStaleMs = 2000`
- `STROKE_TEMPLATES.rhythm`: 횡베기 [0,400] / 사선 [0,0](동시) / 내려찍기 [0,400]
- `STROKE_TEMPLATES.raw`: 각 획의 이상 raw 입력 토큰(패드 대각 단일→카디널 2개 전개)
- **위력+10% 조건**: raw 입력 ≥2 && 등급 ≠ 미스 && 타이밍 최대오차 ≤ 100ms(그레이트 창)

**불변(문서 그대로 유지):** 등급컷 50/70/85/95 · 속도 이상 120~700ms · 최소길이비 0.12 · 거부비 0.035 · 분류거리 임계 0.34 · 콤보창 1500 · 리샘플 64pt · 연풍참 cost 15/window 1.5s · 마나=cost×(2−평균/100) · 평균≥95 위력+20%.

### (c) OI-16 (검결 +10%) 관찰
- **구현 상태**: 다입력(raw≥2)을 타이밍 오차 ≤100ms로 성공 시 `powerBonus`=true, HUD·토스트에 "위력+10%" 표기.
- **관찰**:
  1. 패드 단일 대각(↘ 한 번)은 보너스 제외 → 키보드 동시타·2입력만 보상. "고난도 다입력 보상" 취지에 부합.
  2. 현재 +10%는 **HUD 표기만**, 실제 데미지 미반영(전투 위력 계산은 M1). 최종 "체감"은 실전투 연결 후 판단 가능.
  3. 보너스 임계(≤100ms=그레이트)가 다소 관대 — 정박 근처면 대부분 획득. 난이도 위해 퍼펙트(≤50ms)로 좁히는 것도 검토(실기 튜닝 대상).
  4. `beatMs 400ms`는 임의 초기값 — 느리면 검결이 굼떠 보이고 빠르면 미스 남발. 검술별 상이 템포 필요 가능.
  5. **결론**: 메커니즘 동작·"다입력=보상" 규칙 성립. 유효성 최종 확정은 (1)실데미지 연결 (2)실기 템포 튜닝 (3)테스터 피드백 이후.

### (d) M1 기술 부채 / 리스크
1. **InputArbiter = '무시(drop)' 모델** — 겹친 후발 입력을 큐잉·취소 처리 안 함. 빠른 결전 페이즈에서 의도 입력이 드롭될 수 있음 → M1에서 우선순위/큐/캔슬 정식화.
2. **검결 리듬은 self-referenced**(첫 입력=다운비트) — 외부 메트로놈 클럭에 sync 안 됨. 결전 '합' 페이즈에서 글로벌 비트 동기가 필요하면 재설계.
3. **판정 순수부 + DOM 계층이 단일 index.html에 혼재** — M1 Phaser3+TS 이식 시 engine(순수부) 모듈 분리 + BALANCE/STROKE_TEMPLATES/TECHNIQUES/STYLES를 JSON 외부화. (현재 데이터블록은 JSON화 가능한 형태로 유지 중.)
4. **좌수 미러 시 새 획이 되는 케이스 미구현** — 사선 ↘→↙(diag_dl)는 M0에 없음. M1에서 좌수검류 전용 획 세트 정의 필요.
5. **제스처 템플릿=3획 직선뿐** — 원무(원)·찌르기(짧은 직선)는 분류 임계/속도 재튜닝 필요. 특히 찌르기는 minLenRatio(0.12)와 충돌 소지(짧은 획이 '거부'될 위험).
6. **powerBonus 실데미지 미연결** — 현재 표기만. M1 전투 위력 계산에 실제 곱셈 반영 필요.
7. **콤보창(1500) vs commandResolveMs(640) 상호작용** — 검결 2입력이 640ms까지 늘어져 콤보창을 잠식. 빠른 콤보 요구 시 튜닝.
8. **AudioContext 사용자 제스처 게이팅** — 첫 입력 전 메트로놈 켜면 브라우저 정책상 소리 지연 가능.
9. **테스터 플레이테스트 미실시** — DoD "다시 긋고 싶다" 및 "납득 가능한 실패" 체감은 실사용자 검증 남음(리스크 1순위 대응 항목).

---

- 2026-07-10 [Claude/Opus] **T0-10 좌수 미러 검증**(구현은 T0-04/05에 이미 반영, 이번엔 검증+회귀 고정).
  · 판정 품질 동등성: 캔버스 중심 x미러 입력을 좌수검류로 판정 → 우수검류 원본과 accuracy 동일.
    횡베기 반듯 100=100, **흔들림 60=60(품질 구배까지 보존)**, 내려찍기 100=100.
  · 가상패드 ↑↓ 클러스터: 우수=우측[U,UR,R,D,DR] / 좌수 토글 시 좌측[UL,U,L,DL,D]으로 이동 확인.
  · 라이브 실기: 좌수검류에서 우→좌(미러) → 횡베기 퍼펙트 100 / 좌→우(반대) → 획 불명(미러 정상).
  · 자가진단에 T0-10 4건 추가(20→24/24). node 회귀 29/29.
  · 주의: 사선(↘)은 미러 시 ↙(diag_dl, M0 미존재)이 되어 품질동등 대상 아님 — 정상(M1에서 좌수 사선 획 별도 정의).

- 2026-07-10 [Claude/Opus] **T0-06 InputArbiter 구현**(캔버스/패드 동시 활성, 먼저 든 채널 점유).
  · 시간 기반 blanket 차단(구 arbiterLockMs 110)을 **채널 점유(occupancy) 모델**로 교체:
    행동 시작 채널이('gesture'|'command') 그 행동 종료까지 점유, 종료 시 해제. 백스톱 arbiterStaleMs=2000.
    → 빠른 연속 검결의 2번째가 HUD 갱신을 삼키던 문제 해결(발행은 행동마다 1회, 겹칠 때만 후발 채널 무시).
  · 제스처 좌표를 offsetX → clientX−rect 로 변경(변환/DPR 안전 + 합성 PointerEvent 테스트 가능).
  · 검증: 자가진단 20/20, ①패드 ↘×2(50ms) 둘 다 발행(사선 숙련+10) ②혼합입력 연풍참(제스처 횡베기→검결 사선, 평균100·위력+20%)
    ③커맨드 대기중 제스처 무시→해제 후 정상. JS 에러 없음.
  · 미완/주의: 점유 중 후발 채널 입력은 '무시'(큐잉 아님) — 의도적. arbiterStaleMs는 안전 백스톱일 뿐 정상 경로 아님.

- 2026-07-10 [Claude/Opus] **T0-05 RhythmJudge(검결 타이밍 판정) 구현 + GitHub Pages 배포**.
  · 배포: github.com/hermit7999/swordmaster-road 신규 생성, main/root Pages 활성화 →
    **라이브 https://hermit7999.github.io/swordmaster-road/** (build=built, HTTP 200 확인).
  · T0-05: 순수 judgeRhythm(rawInputs, strokeId, style) — 이상 비트 타임라인 대비 타이밍 오차.
    퍼펙트±50/그레이트±100/굿±180/배드±280ms, 순서·개수 오류=미스. STROKE_TEMPLATES에 raw/rhythm(오프셋) 추가,
    BALANCE.rhythm{beatMs:400}. 다입력(고난도) 정밀 성공 시 **위력+10%**(패드 단일 대각 press는 제외).
  · **버그 수정(중요)**: 정박 간격(400ms) > 커맨드 확정대기(260ms)라 2번째 비트 전에 버퍼가 쪼개져
    "정박 플레이 불가"였음. → **완전 일치 시 즉시 확정**(타이밍은 타임스탬프로 보존) + 확정대기 640ms 상향.
  · 메트로놈(목검 비트) 추가: Web Audio 틱 + 비트 펄스 + 토글 버튼(beatMs 기준).
  · resolveCommand가 임시점수 대신 judgeRhythm 실판정 사용. 검결 정확도 라벨의 "T0-05 대기" 문구 제거.
  · 검증: node 회귀 24/24, 브라우저 자가진단 20/20, 실기 타이밍(정박400=퍼펙트+보너스 / 470=그레이트 /
    150=배드 / 동시사선=퍼펙트+보너스) 실증. 메트로놈 토글·펄스 동작. JS 에러 없음.
  · 미완/주의: T0-06(InputArbiter)는 근접중복 차단(arbiterLockMs 110) 수준 임시 — 빠른 연속 검결이 HUD갱신을 삼킬 수 있음(다음 과제).

- 2026-07-10 [Claude/Opus] M0 베이스 재구성 + **T0-04 검결(劍訣) CommandCapture 구현**.
  · 이전 세션의 M0 index.html이 이 PC에 없어(디스크 미저장) 인수인계 문서 스펙대로 단일 index.html 충실 재구성:
    T0-01 판정데이터(획3종 h_lr/diag_dr/v_down + BALANCE), T0-02 Pointer 궤적수집,
    T0-03 순수 judgeStroke(), T0-07 등급별 검명5종+햅틱, T0-08 이상/실제 비교 오버레이, T0-09 연풍참 콤보+마나공식.
  · **T0-04 신규**: (a) 키보드 방향키 버퍼링 — 순차(←→)/동시키(↓+→ → 대각 접기) 인식, e.repeat 무시.
    (b) 가상 방향패드(멀티터치, 좌 ↖←↙ / 우 ↗→↘, ↑↓=유파 클러스터). 산출=(획ID 후보, 타임스탬프) 시퀀스.
  · 판정 이벤트 규격 {strokeId,accuracy,grade,inputMode,timestamp} 고정. 방향점수는 국소 방향 일치도로 모델링(흔들림→배드 구배).
  · 유파 미러(mirrorX) 훅을 판정/커맨드 양쪽에 추가(T0-10 대비 groundwork, 우수/좌수 토글 동작).
  · **설계 변경(사용자 승인 2026-07-10): 방향 점수(40%) 모델 = 순(net) 시작→끝 벡터 → 구간별 국소 방향 일치도.**
    사유: 순 벡터로 재면 좌→우로 흔들려 그어도 시작·끝점만 맞으면 방향 40점이 항상 만점 →
    방향40+속도15+완주15=70점 바닥이 보장되어 "흔들린 획"이 배드(50–69)로 내려갈 수 없었다.
    "정확도=궤적 정밀도"(SRS)의 취지에 어긋남. 변경 내용: judgeStroke에서 리샘플된 각 구간(segment)의
    단위벡터와 템플릿 방향의 내적을 clamp(0..) 평균 → 국소적으로 흐트러지면 방향점수가 하락.
    효과: 흔들림 강도에 따라 그레이트→굿→배드로 연속 감점(node 검증 jitter 20/40/60/80/100 = 91/73/60/54/50점).
    등급컷·가중치(40/30/15/15) 등 BALANCE 수치는 불변, 방향 산식만 교체(FR-JDG-004).
  · 검증: node 회귀 16/16, 브라우저 자가진단 14/14, 키보드로 연풍참 성립 실증(평균92·마나16.2). JS 에러 없음.
  · 미완/주의: 검결 정확도는 T0-05(RhythmJudge) 전까지 tightness 기반 **임시점수**(화면에 라벨 명시).
    T0-06(InputArbiter)는 근접중복 차단 수준의 임시 구현. 원무/찌르기 등 5획은 M1 범위(제외).
    실기 검증(폰/PC 실제 그어보기)·GitHub Pages 배포는 미실시(원격 미설정, push는 사용자 승인 대기).
